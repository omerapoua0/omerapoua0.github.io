const automationAnswers = {research:['Research loop','A signal system that watches competitors, sources or markets, filters the noise and puts the important change in front of the right person.'],handoffs:['Workflow connection','A clear, traceable path between the tools you already use — without copy-pasting, lost context or status-chasing.'],decisions:['Decision rhythm','A live operating view with the right trigger, model or message when an action is needed — not after the window has closed.']};
document.querySelectorAll('[data-auto]').forEach(button => button.addEventListener('click', () => { const [label, answer] = automationAnswers[button.dataset.auto]; document.querySelectorAll('[data-auto]').forEach(x => x.classList.remove('selected')); button.classList.add('selected'); document.getElementById('auto-label').textContent = label; document.getElementById('auto-answer').textContent = answer; }));
